<%= form_for(@work, :html => {:class => 'form-inline'}) do |f| %>
  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.select :doctor_id, options_for_select(@doctors.collect { |doctor|
    [doctor.user.name.titleize, doctor.id] }, 1), {}, { id: 'works_doctors_select', :class => 'form-group' } %>
  <%= f.select :clinic_id, options_for_select(@clinics.collect { |clinic|
    [clinic.name.titleize, clinic.id] }, 0), {}, { id: 'clinics_select', :class => 'form-group' } %>

  <div class="actions">
    <%= f.submit "Create", :class => "btn btn-primary" %>
  </div>
<% end %>
